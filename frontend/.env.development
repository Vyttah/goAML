# Dev defaults. The SPA talks to the backend through Vite's dev proxy (see vite.config.ts),
# so API calls use same-origin relative paths and VITE_API_BASE_URL stays empty.
# Point the SPA at a remote backend by setting VITE_API_BASE_URL (the env-gated CORS bean must allow it).
VITE_API_BASE_URL=
