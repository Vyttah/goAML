apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "goaml.fullname" . }}-config
  labels:
    {{- include "goaml.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
