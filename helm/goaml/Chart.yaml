apiVersion: v2
name: goaml
description: goAML platform — multi-tenant RegTech reporting service (REST API + React SPA in one image).
type: application
version: 0.1.0
appVersion: "0.1.0"
kubeVersion: ">=1.27.0-0"
maintainers:
  - name: Vyttah
