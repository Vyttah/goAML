<?xml version="1.0" encoding="UTF-8"?>
<!--
  Logging config (Phase 14 observability).
   - default / non-prod: the human-readable console pattern (honors logging.pattern.console from
     application.yml via CONSOLE_LOG_PATTERN), including the %X{correlationId} MDC field.
   - `prod` profile: structured JSON to stdout (logstash-logback-encoder) for log aggregation, with the
     correlationId MDC included. Activate with SPRING_PROFILES_ACTIVE=prod.
-->
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <springProfile name="!prod">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>${CONSOLE_LOG_PATTERN}</pattern>
                <charset>UTF-8</charset>
            </encoder>
        </appender>
    </springProfile>

    <springProfile name="prod">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdcKeyName>correlationId</includeMdcKeyName>
                <customFields>{"app":"goaml"}</customFields>
            </encoder>
        </appender>
    </springProfile>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
