package com.vyttah.goaml.domain.enums;

/**
 * goAML submission type. {@code E} = electronic, {@code M} = manual.
 * (The schema also defines {@code -} for unknown; not used in our flows.)
 */
public enum SubmissionCode {
    E, M;
}
